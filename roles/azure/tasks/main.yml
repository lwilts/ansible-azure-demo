---
- name: Create VM and supporting resources
  include: create.yml
  when: (delete is undefined) or (not delete)

- name: Delete VM, NIC and storage
  include: destroy.yml
  when: (delete is defined) and (delete)
